name: CI
on: [push, pull_request]
jobs:
  perft:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: make -j
      - name: Perft suite
        run: ./tests/perft_suite.sh
